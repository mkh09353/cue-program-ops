import { useEffect, useState } from "react";
import { api } from "../lib/api";
import { Badge, Button, Card, Field, Input, PageHeader, Select } from "../components/ui";

function useData(load:()=>Promise<any>){const[data,setData]=useState<any[]>([]),[error,setError]=useState("");const reload=()=>load().then(r=>setData(r.data)).catch(e=>setError(e.message));useEffect(()=>{void reload()},[]);return{data,error,reload}}
function RoundEditor({round,personas,saved}:{round:any;personas:any[];saved:()=>void}){const[draft,setDraft]=useState(()=>structuredClone(round));const[invite,setInvite]=useState({name:"",email:""});const criteria=draft.criteria||[];return <Card className="p-5"><div className="grid gap-3 md:grid-cols-2"><Field label="Round name"><Input value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})}/></Field><Field label="Blind review"><Select value={String(draft.blind)} onChange={e=>setDraft({...draft,blind:e.target.value==="true"})}><option value="false">Identity visible</option><option value="true">Blind</option></Select></Field><Field label="Opens"><Input type="datetime-local" value={draft.opensAt?.slice(0,16)} onChange={e=>setDraft({...draft,opensAt:new Date(e.target.value).toISOString()})}/></Field><Field label="Closes"><Input type="datetime-local" value={draft.closesAt?.slice(0,16)} onChange={e=>setDraft({...draft,closesAt:new Date(e.target.value).toISOString()})}/></Field></div><h3 className="mt-4 text-xs font-bold uppercase tracking-wide text-mid">Scorecard criteria</h3>{criteria.map((c:any,i:number)=><div key={c.id} className="mt-2 grid gap-2 md:grid-cols-[1fr_140px_100px_auto]"><Input aria-label="Criterion label" value={c.label} onChange={e=>setDraft({...draft,criteria:criteria.map((x:any,n:number)=>n===i?{...x,label:e.target.value}:x)})}/><Select value={c.type} onChange={e=>setDraft({...draft,criteria:criteria.map((x:any,n:number)=>n===i?{...x,type:e.target.value}:x)})}><option value="rating">Rating</option><option value="select">Select</option><option value="text">Text</option></Select><Input aria-label="Criterion weight" type="number" min="0" step="0.5" value={c.weight} onChange={e=>setDraft({...draft,criteria:criteria.map((x:any,n:number)=>n===i?{...x,weight:Number(e.target.value)}:x)})}/><Button variant="outline" onClick={()=>setDraft({...draft,criteria:criteria.filter((_:any,n:number)=>n!==i)})}>Remove</Button></div>)}<Button className="mt-2" size="sm" variant="outline" onClick={()=>setDraft({...draft,criteria:[...criteria,{id:`criterion-${Date.now()}`,label:"New criterion",type:"rating",weight:1}]})}>Add criterion</Button><h3 className="mt-4 text-xs font-bold uppercase tracking-wide text-mid">Reviewer pool</h3><div className="mt-2 flex flex-wrap gap-2">{personas.filter(p=>p.role==="reviewer").map(p=><label key={p.id} className="rounded-full border px-3 py-1 text-sm"><input className="mr-2" type="checkbox" checked={draft.reviewerIds.includes(p.id)} onChange={e=>setDraft({...draft,reviewerIds:e.target.checked?[...draft.reviewerIds,p.id]:draft.reviewerIds.filter((id:string)=>id!==p.id)})}/>{p.name}</label>)}</div><div className="mt-3 grid gap-2 md:grid-cols-[1fr_1fr_auto]"><Input placeholder="Reviewer name" value={invite.name} onChange={e=>setInvite({...invite,name:e.target.value})}/><Input type="email" placeholder="Reviewer email" value={invite.email} onChange={e=>setInvite({...invite,email:e.target.value})}/><Button variant="outline" onClick={async()=>{await api.inviteReviewer(round.id,invite);setInvite({name:"",email:""});saved()}}>Invite reviewer</Button></div><Button className="mt-4" onClick={async()=>{await api.updateReviewRound(round.id,draft);saved()}}>Save round</Button></Card>}
export function EvaluationPlanPage(){const{data:rounds,error,reload}=useData(api.reviewRounds),{data:boot,reload:reloadBoot}=useData(async()=>{const x=await api.bootstrap();return{data:x.data.personas}});const[name,setName]=useState("");const refresh=()=>{reload();reloadBoot()};return <div><PageHeader title="Evaluation Plan" description="Edit dates, blind review, reviewer pools, and weighted scorecards." actions={<Button onClick={async()=>{if(!name)return;await api.createReviewRound({name,status:"draft",blind:false,reviewerIds:[],criteria:[]});setName("");refresh()}}>Add round</Button>}/>{error&&<p>{error}</p>}<div className="mb-4 max-w-sm"><Field label="New round name"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Committee Decision"/></Field></div><div className="space-y-3">{rounds.map(r=><RoundEditor key={r.id} round={r} personas={boot} saved={refresh}/>)}</div></div>}
export function AssignmentsPage(){const{data:rounds}=useData(api.reviewRounds),{data:subs}=useData(api.submissions),{data:boot}=useData(async()=>{const x=await api.bootstrap();return{data:x.data.personas}});const[roundId,setRound]=useState("round-initial"),[reviewerId,setReviewer]=useState("rev-ada"),[cap,setCap]=useState(5),[selected,setSelected]=useState<string[]>([]),[message,setMessage]=useState("");const run=async(method:string)=>{const ids=method==="specific"?selected:subs.map(s=>s.id);const r=await api.assignReviews({roundId,reviewerId,submissionIds:ids,method,cap});setMessage(`${r.data.length} assignments created`)};return <div><PageHeader title="Assignments" description="Choose abstracts and assign them to a reviewer."/><Card className="grid gap-4 p-5 md:grid-cols-3"><Field label="Round"><Select value={roundId} onChange={e=>setRound(e.target.value)}>{rounds.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</Select></Field><Field label="Reviewer"><Select value={reviewerId} onChange={e=>setReviewer(e.target.value)}>{boot.filter(p=>p.role==="reviewer").map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</Select></Field><Field label="Per-reviewer limit"><Input type="number" min={1} value={cap} onChange={e=>setCap(Number(e.target.value))}/></Field><div className="flex gap-2"><Button disabled={!selected.length} onClick={()=>run("specific")}>Assign selected ({selected.length})</Button><Button variant="outline" onClick={()=>run("auto")}>Auto-distribute</Button></div>{message&&<p className="text-sm font-semibold">{message}</p>}</Card><div className="mt-4 overflow-x-auto rounded-[24px] border bg-paper"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Select</th><th>Submission</th><th>Track</th><th>Status</th></tr></thead><tbody>{subs.map(s=><tr key={s.id} className="border-b"><td className="p-3"><input aria-label={`Select ${s.title}`} type="checkbox" checked={selected.includes(s.id)} onChange={e=>setSelected(e.target.checked?[...selected,s.id]:selected.filter(id=>id!==s.id))}/></td><td><b>{s.title}</b><div className="text-xs text-mid">{s.name}</div></td><td>{s.category}</td><td><Badge>{s.status}</Badge></td></tr>)}</tbody></table></div></div>}
export function ReviewProgressPage(){const{data,reload}=useData(api.reviewProgress);const[message,setMessage]=useState("");const outstanding=data.filter(r=>r.outstanding).map(r=>r.reviewerId);return <div><PageHeader title="Review Progress" description="Live completion by round and reviewer." actions={<Button disabled={!outstanding.length} onClick={async()=>{const r=await api.reviewReminders([...new Set(outstanding)] as string[]);setMessage(`${r.data.length} reminder(s) logged`);reload()}}>Remind outstanding reviewers</Button>}/>{message&&<p>{message}</p>}{data.map(r=><Card className="mt-2 flex justify-between p-4" key={`${r.roundId}-${r.reviewerId}`}><b>{r.reviewer?.name||r.reviewerId}</b><span>{r.completed} of {r.assigned} complete · {r.percent}%</span></Card>)}</div>}
export function ResultsPage(){const{data}=useData(api.reviewResults);return <div><PageHeader title="Results" description="Human review aggregates and disposition." actions={<Button onClick={()=>window.location.assign(api.reviewResultsCsv())}>Export CSV</Button>}/><div className="overflow-x-auto rounded-[24px] border bg-paper"><table className="w-full text-left text-sm"><thead><tr><th className="p-3">Submission</th><th>Score</th><th>Reviews</th><th>Status</th></tr></thead><tbody>{data.map(r=><tr className="border-t" key={r.id}><td className="p-3"><b>{r.title}</b></td><td>{r.aggregateScore?.toFixed(2)||"—"}</td><td>{r.reviewerCount}</td><td><Badge>{r.status}</Badge></td></tr>)}</tbody></table></div></div>}
